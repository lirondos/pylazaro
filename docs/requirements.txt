# docs/requirements.txt
# The heavy runtime dependencies (torch, flair, spacy, transformers) are mocked in
# conf.py via autodoc_mock_imports, so they are not installed to build the docs.
sphinx>=5.0,<8
sphinx_rtd_theme
attrs
